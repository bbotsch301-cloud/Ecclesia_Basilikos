# Replace :80 with your domain (e.g., ecclesiabasilikos.com) to enable automatic HTTPS
:80 {
	reverse_proxy app:5000

	encode gzip zstd

	header {
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
		-Server
	}
}
